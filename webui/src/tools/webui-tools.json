[
  {
    "name": "webui_camera_take_photo",
    "description": "Take a photo using the WebUI's camera. This tool captures the current camera frame and returns the image data. You MUST call this tool when user asks to 'take a photo', 'what do you see', 'describe what you see', or similar requests. After getting the photo data, describe what you see in the image.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "question": {
          "type": "string",
          "description": "Optional question to guide the photo analysis"
        }
      },
      "required": []
    }
  }
]